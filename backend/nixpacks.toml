[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -m py_compile server.py"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
