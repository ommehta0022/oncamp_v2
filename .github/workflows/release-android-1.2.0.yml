name: Legacy OnCampus Android 1.2.0 Baseline

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "OnCampus 1.2.0 is a retired baseline. Build 1.3.0 or newer for native releases."
